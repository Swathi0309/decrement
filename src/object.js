import React from 'react';

const names=["swathi","swapna","sruthi","shivani","sony","siri"];

function Names(){
    return(
        <div>
           {names.unshift(("uma",value=>(
            <p></p>
           )))}
        </div>
    )
}

export default Names;
//it's print length of update array unshif means add element at first position